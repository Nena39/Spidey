services:
- type: web
  name: app-backend
  env: go
  rootDir: backend
  buildCommand: go build -o app .
  startCommand: ./app
- type: web
  name: app-community
  env: docker
  rootDir: community/docker
  dockerfilePath: ./Dockerfile
  dockerContext: .
